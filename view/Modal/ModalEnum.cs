namespace Tviewer.view.Modal
{
    public enum AnimationState
    {
        Ready,
        Playing,
        Waiting
    }

    public enum ModalAnimation
    {
        None,
        FloatingUp
    }

    public enum ModalEventArgs
    {
        Add,
        Delete
    }
}
